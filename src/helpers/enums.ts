export enum VoteOptions {
  YES = 'yes',
  NO = 'no',
  ABSTAIN = 'abstain'
}

export enum AuthLevels {
  ADMIN = 'ADMIN',
  VOTER = 'VOTER',
  AUDIENCE = 'AUDIENCE'
}

export enum Directions {
  EARLIER = 'EARLIER',
  LATER = 'LATER'
}
